package entity;
/**
 * Enum {@code EntityType} is entity class.
 * It stores types of entities, which exists in project.
 *
 * @author Yahor Makedon
 * @see entity
 * @version 1.0
 * @since version 1.0
 */
public enum EntityType {
    COUNTRY, HOTEL, REVIEW, TOUR, TOUR_TYPE, USER
}
